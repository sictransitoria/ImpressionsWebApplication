<!DOCTYPE html>
	<html>
	<head>
		<title>Impressions | A Photo Sharing App</title>
	</head>
	<body>

		<h1>Welcome to Impressions.</h1>
		<a href="/profile"><button>Profile</button></a>

		<% for(let i in rows) { %>
		
	<div>
		<img src="thumbnails/<%= rows[i].dataValues.image %>">
	</div>
	
		<p><%= rows[i].dataValues.username %></p>
		<p><%= rows[i].dataValues.comment %></p> 

		<% } %>

		<a href="/logout"><button>Logout</button></a>

	</body>
</html>